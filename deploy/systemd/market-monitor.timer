[Unit]
Description=Run the market monitor at the configured Tehran slots

[Timer]
# Must fire at the union of [schedule].snapshot and [schedule].analysis in
# config/default.toml.
OnCalendar=*-*-* 09,13,17,21:00:00
Persistent=true

[Install]
WantedBy=timers.target
